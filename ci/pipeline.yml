resources:
- name: git-repo
  type: git
  source:
    uri: https://github.com/nwoike/spring-music.git
    branch: ci

jobs:
- name: build
  plan:
  - get: git-repo
    trigger: true
  - task: package
    privileged: true
    file: git-repo/ci/build.yml

